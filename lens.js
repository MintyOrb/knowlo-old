var disciplines = keywords[1]
for (var index = 0; index < disciplines.members.length; index++) {
  disciplines.members[index].translation = disciplines.members[index]
  disciplines.members[index].term = disciplines.members[index]
}

var bigHistory = keywords[0]

for (var index = 0; index < bigHistory.members.length; index++) {
  bigHistory.members[index].translation = bigHistory.members[index]
  bigHistory.members[index].term = bigHistory.members[index]
}
